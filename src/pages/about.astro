---
// src/pages/about.astro
// 私たちについてページ
import Navigation from '../components/Navigation.astro';
---

<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>われわれについて | INU-Hacker 🐕</title>
  <meta name="description" content="INU-Hackerの代表メンバーと会社概要をご紹介します">

  <!-- OGP -->
  <meta property="og:title" content="われわれについて | INU-Hacker">
  <meta property="og:description" content="INU-Hackerの代表メンバーと会社概要をご紹介します">
  <meta property="og:type" content="website">

  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">

  <style>
    @import url('https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c:wght@400;700;900&display=swap');

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'M PLUS Rounded 1c', -apple-system, BlinkMacSystemFont, sans-serif;
      line-height: 1.8;
      background: #0a0e27;
      color: #e8eaf6;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }

    /* Hero Section */
    .hero {
      background: linear-gradient(135deg, #1a1f3a 0%, #2d1b4e 50%, #1e3a5f 100%);
      padding: 6rem 0 8rem;
      position: relative;
      overflow: hidden;
      text-align: center;
    }

    .hero::before {
      content: "🐕";
      position: absolute;
      top: 20%;
      left: 10%;
      font-size: 8rem;
      opacity: 0.1;
      animation: float 4s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px) rotate(0deg); }
      50% { transform: translateY(-20px) rotate(5deg); }
    }

    .hero h1 {
      font-size: clamp(2.5rem, 6vw, 4rem);
      font-weight: 900;
      margin-bottom: 1.5rem;
      color: #ffffff;
      text-shadow: 0 0 30px rgba(139, 92, 246, 0.3);
    }

    .hero-subtitle {
      font-size: 1.3rem;
      color: #c4b5fd;
      max-width: 700px;
      margin: 0 auto;
      line-height: 1.8;
    }

    /* Section */
    section {
      padding: 5rem 0;
    }

    .section-title {
      font-size: 2.5rem;
      font-weight: 900;
      text-align: center;
      margin-bottom: 1rem;
      color: #ffffff;
    }

    .section-subtitle {
      text-align: center;
      color: #a5b4fc;
      margin-bottom: 4rem;
      font-size: 1.1rem;
    }

    /* Members */
    .members-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 3rem;
      max-width: 900px;
      margin: 0 auto;
    }

    .member-card {
      background: rgba(30, 41, 59, 0.6);
      backdrop-filter: blur(10px);
      border-radius: 24px;
      padding: 3rem 2.5rem;
      border: 2px solid rgba(139, 92, 246, 0.2);
      box-shadow: 0 4px 20px rgba(0,0,0,0.3);
      transition: all 0.3s ease;
      text-align: center;
    }

    .member-card:hover {
      transform: translateY(-8px);
      border-color: rgba(139, 92, 246, 0.5);
      box-shadow: 0 12px 40px rgba(139, 92, 246, 0.3);
    }

    .member-avatar {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      margin: 0 auto 2rem;
      box-shadow: 0 8px 30px rgba(139, 92, 246, 0.5);
      overflow: hidden;
      background: transparent;
    }

    .member-avatar img {
      width: 100%;
      height: 100%;
      object-fit: contain;
      display: block;
    }

    /* アバター画像がない場合のフォールバック */
    .member-avatar-placeholder {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      background: linear-gradient(135deg, #8b5cf6 0%, #ec4899 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 5rem;
      margin: 0 auto 2rem;
      box-shadow: 0 8px 30px rgba(139, 92, 246, 0.5);
    }

    .member-name {
      font-size: 2rem;
      font-weight: 900;
      margin-bottom: 0.5rem;
      color: #ffffff;
    }

    .member-role {
      font-size: 1.1rem;
      font-weight: 700;
      color: #a78bfa;
      margin-bottom: 1.5rem;
    }

    .member-bio {
      color: #c7d2fe;
      line-height: 1.9;
      font-size: 1.05rem;
      text-align: left;
    }

    /* Company Info */
    .company-info {
      max-width: 800px;
      margin: 0 auto;
      background: rgba(30, 41, 59, 0.6);
      backdrop-filter: blur(10px);
      border-radius: 24px;
      padding: 3rem;
      border: 2px solid rgba(139, 92, 246, 0.2);
      box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    }

    .info-table {
      width: 100%;
    }

    .info-row {
      display: grid;
      grid-template-columns: 180px 1fr;
      padding: 1.5rem 0;
      border-bottom: 1px solid rgba(139, 92, 246, 0.2);
    }

    .info-row:last-child {
      border-bottom: none;
    }

    .info-label {
      font-weight: 900;
      color: #a78bfa;
      font-size: 1.1rem;
    }

    .info-value {
      color: #e0e7ff;
      font-size: 1.05rem;
      line-height: 1.8;
    }

    /* CTA Section */
    .cta-section {
      background: linear-gradient(135deg, #8b5cf6 0%, #ec4899 100%);
      color: white;
      border-radius: 32px;
      padding: 4rem 2rem;
      margin: 0 1rem;
      text-align: center;
      box-shadow: 0 10px 40px rgba(139, 92, 246, 0.5);
    }

    .cta-title {
      font-size: 2.5rem;
      font-weight: 900;
      margin-bottom: 1rem;
    }

    .cta-description {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      opacity: 0.95;
    }

    .btn {
      display: inline-block;
      padding: 1.2rem 3rem;
      font-size: 1.1rem;
      font-weight: 700;
      text-decoration: none;
      border-radius: 50px;
      transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
      border: none;
      cursor: pointer;
    }

    .btn:hover {
      transform: translateY(-3px) scale(1.05);
      box-shadow: 0 8px 20px rgba(0,0,0,0.2);
    }

    .btn-white {
      background: white;
      color: #8b5cf6;
      box-shadow: 0 4px 20px rgba(255,255,255,0.4);
    }

    .btn-white:hover {
      background: rgba(255, 255, 255, 0.95);
      color: #ec4899;
    }

    @media (max-width: 768px) {
      .hero {
        padding: 4rem 0 5rem;
      }

      .hero h1 {
        font-size: 2rem;
      }

      .section-title {
        font-size: 2rem;
      }

      .members-grid {
        grid-template-columns: 1fr;
      }

      .info-row {
        grid-template-columns: 1fr;
        gap: 0.5rem;
      }

      .info-label {
        font-size: 1rem;
      }

      .info-value {
        font-size: 0.95rem;
      }

      .cta-title {
        font-size: 1.8rem;
      }

      .nav-links {
        gap: 1rem;
      }

      .nav-link {
        font-size: 0.9rem;
        padding: 0.4rem 0.8rem;
      }

      .nav-links {
        gap: 1rem;
      }

      .nav-link {
        font-size: 0.9rem;
        padding: 0.4rem 0.8rem;
      }

      .company-info {
        padding: 2rem 1.5rem;
      }
    }
  </style>
</head>
<body>
<Navigation />

<main>
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <h1>われわれについて</h1>
      <p class="hero-subtitle">
        いぬハッカーは、テクノロジーの力で<br>
        人々の暮らしをちょっと楽しく、ちょっと便利にする<br>
        そんなWebサービスを作っている小さなチームです
      </p>
    </div>
  </section>

  <!-- Members -->
  <section>
    <div class="container">
      <h2 class="section-title">代表メンバー</h2>
      <p class="section-subtitle">ふたりで楽しく、丁寧にものづくりをしています</p>

      <div class="members-grid">
        <div class="member-card">
          <div class="member-avatar">
            <img src="https://inu-hacker.com/miCKU_CS_400x400.jpg" alt="@pinknoisebabies">
          </div>
          <h3 class="member-name">@pinknoisebabies</h3>
          <p class="member-role">共同代表 / エンジニア</p>
          <p class="member-bio">
            「天国が落ちる場所」で会いましょう
          </p>
        </div>

        <div class="member-card">
          <div class="member-avatar">
            <img src="https://inu-hacker.com/IDEP0m0d_400x400.jpg" alt="@toofu">
          </div>
          <h3 class="member-name">@toofu</h3>
          <p class="member-role">共同代表 / デザイナー</p>
          <p class="member-bio">
            シーランド公国伯爵
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Company Info -->
  <section style="background: linear-gradient(135deg, #1e293b 0%, #334155 100%);">
    <div class="container">
      <h2 class="section-title">会社概要</h2>

      <div class="company-info">
        <div class="info-table">
          <div class="info-row">
            <div class="info-label">会社名</div>
            <div class="info-value">合同会社INU-Hacker（いぬハッカー）</div>
          </div>

          <div class="info-row">
            <div class="info-label">設立</div>
            <div class="info-value">2023年6月</div>
          </div>

          <div class="info-row">
            <div class="info-label">事業内容</div>
            <div class="info-value">
              Webサイト・Webアプリケーション開発<br>
              受託開発<br>
              技術コンサルティング
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section style="padding: 6rem 0;">
    <div class="cta-section">
      <h2 class="cta-title">お問い合わせ</h2>
      <p class="cta-description">
        プロジェクトのご相談、お見積もり、ご質問など<br>
        お気軽にメールでご連絡ください
      </p>
      <a href="mailto:contact@inu-hacker.com" class="btn btn-white">📧 contact@inu-hacker.com</a>
    </div>
  </section>
</main>
</body>
</html>